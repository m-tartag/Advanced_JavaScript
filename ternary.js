// check if num is positive, negtive, or zero
// positive
// neg
// zero

function checkSign(num) {
  console.log(num >= 0 ? "positive" : "negative");
}

checkSign(-3);
checkSign(3);

const x = "Gamer";
console.log(x[x.length - 2]);
